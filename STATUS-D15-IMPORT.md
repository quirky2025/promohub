# D15 导入批次 — 问题总清单与状态(2026-07-24)

> 这批(D15):7月22日导入,共 **147 个新品**(Trends 87 + PromoBrands 61,去重后 147)。
> 状态图例:✅ 已解决 · 🟡 部分/待执行 · ❌ 未解决 · 🔎 待你确认

---

## 一、价格翻倍(最严重)

**问题**:导入器把供应商成本 **多乘了一次 margin** 存进库(`base_price = 成本 × tierMargin`),前台再乘一次 → 客户看到的是 **成本 × margin²**。
实证:S777 Byron 瓶,成本 $14.96 → 库里存 $22.44 → 前台显示 $33.66(应为 ~$22.44)。印刷价同理(Pad Print 成本 $0.40 → 显示 $0.90)。

| 步骤 | 状态 |
|---|---|
| 定位根因(导入器 390/591/400/410/621 行) | ✅ 已完成 |
| 修好导入器代码(改存 raw 成本,加价交前台) | ✅ 已改好(本地文件) |
| **push + Vercel 部署** | ❌ **还没做** —— 这步不做,线上跑的还是坏代码 |
| 删除坏批次 → 重导 | ❌ 未做(要等部署后) |
| 重导后验证 S777 = $14.96 | ❌ 未做 |

---

## 二、图片问题

**问题**:你说"图片有好多问题",但**还没告诉我具体是什么**(没图 / 错图 / 图裂)。
- 状态:❌ **未诊断** —— 等你发一张截图或一句话描述,我去查导入器图片代码(`imageToR2` / gallery 那段)。

---

## 三、重复产品

| 问题 | 状态 |
|---|---|
| SKU 完全重复 | ✅ 无重复(147 个 SKU 全唯一) |
| 名字完全相同:**"Universal Magnetic Car Mount" ×2**(FD400.MTO + FD405.MTO,都在 Bags) | 🔎 待你确认是否同一产品、删一个 |

---

## 四、类目映射错误

| 问题 | 状态 |
|---|---|
| "Universal Magnetic Car Mount"(车载支架)被归到 **Bags** | ❌ 未改 |
| "Aero Flyer - Small" 在 Headwear、"- Large" 在 Toys & Games(同产品分到两类) | ❌ 未改 |
| 可能还有其它误映射(147 个没逐个核类目) | ❌ 未全查 |

---

## 五、无价产品 → quote_only(询价)

**背景**:有些产品供应商没给价(导入日志显示 `tiers 为空` 警告),应走"Price on application"询价模式。

| 步骤 | 状态 |
|---|---|
| 导入器:无价自动标 `quote_only=true` | 🟡 那边加过,我核对过逻辑对,**但随价格 bug 一起要重导才生效** |
| 前端 PDP 询价模式(显示 Price on application) | ✅ 早已存在,正常 |
| **列表卡片仍会显示残留 "From $X"**(卡片没判断 quote_only) | ❌ **未修** —— 我上次指出的缺口,还没动 |

---

## 六、安全:PROBE_KEY 口令暴露

**问题**:导入网址的口令在聊天里暴露过(`4ebc…`),别人拿到能触发你的导入(碰不到客户数据/钱)。
- 状态:❌ **未更换** —— 回来去 Vercel → Settings → Environment Variables 把 `PROBE_KEY` 改成新的随机值,再 Redeploy。低风险,但要做。

---

## ✅ 已经做好、保护住你的:

- **整批已下线**:`update products set is_published=false where created_at >= '2026-07-22'` 已执行成功。
  → 这 147 个客户**全部看不到**,任何价格/图片/类目错误都**碰不到客户、碰不到订单、碰不到钱**。危机已止血。
- 导入器修复代码已写好(就差 push)。
- 重复、类目问题已排查列出。

---

## 📋 回来后的执行顺序(从上到下)

1. **push 修好的导入器 + 等 Vercel 部署**(我给命令)。
2. **小验证**:删 S777 一个 → 重导 → 查 base_price = 14.96(确认修复上线)。
3. **删整批 → 重导**(147 个,我给 SQL + 网址步骤)。
4. **重导后抽查**:价格对了、图片对了、quote_only 产品显示询价。
5. **修列表卡片**残留 From(代码,我做)。
6. **改类目**(车载支架、Aero Flyer 等)。
7. **删重复**(确认 Car Mount 那两个)。
8. **换 PROBE_KEY**(Vercel)。

---

## 🔧 另外还挂着的(和 D15 无关,那个 chat 做过、需我逐个核对)

- PO 分行录入(和供应商发票一致)—— 🟡 待核对
- NOTIFY DELIVERED 不自动变 Delivered —— 🟡 待核对
- 按产品发 Review 邀请邮件 —— 🟡 待核对
- 首页/品牌/系列 Banner 收尾 —— 🟡 部分完成

> 你之前说这 5 项"已做了",但那个 chat 出过错,我会**逐个打开核对**再告诉你哪些真的成了、哪些有问题。
